#include <stdio.h>
#include <stdlib.h>

int main(){

    // Variável é um local na memória onde armazenar-se uma informação.

    int i = 2;
    printf("O valor contido sera de: %d", i);

    /*

        Características das variáveis:
        1) Toda variável possui um nome
        2) Toda variável possui um tipo
        3) Toda variável possui um tamanho
        4) Toda variável possui um valor (mesmo se for vazio ou nulo)

        Características do armazenamento na memória:
        1) O armazenamento é temporário
        2) Quando um programa é fechado ou o computador é desligado, tudo que estiver na memória não será salvo
        3) Os valores armazenados na memória são voláteis
        4) O valor antigo da variável será sobreposto se for atribuído um novo valor

    */

    return 0;
}
